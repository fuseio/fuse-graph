{
  "name": "fuse-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ fuseio/fuse",
    "create-local": "graph create --node http://localhost:8020/ fuseio/fuse",
    "remove-local": "graph remove --node http://localhost:8020/ fuseio/fuse",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fuseio/fuse"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.3",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
